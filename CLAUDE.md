# Working conventions

## Branch naming

When creating a branch to cover a whole session's worth of varied work
(not a single targeted change), name it `sessionYYYYMMDD`, using the date
the branch was created — e.g. `session20260722`. This does not apply to
branches for a specific, narrowly-scoped piece of work; name those
descriptively instead.
